[package]
name = "gpui-counter"
version = "0.1.0"
edition = "2021"
description = "A tiny counter app built with Zed's GPUI framework"
license = "Apache-2.0"

[dependencies]
gpui = "0.2.2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
